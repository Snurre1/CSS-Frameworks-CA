/*import { API_SOCIAL_URL } from "./api/constants.mjs";
const action = "/posts";
async function fetchingPosts() {
  try {
    const response = await fetch(API_SOCIAL_URL + action);
    const result = await response.json();
    console.log(result);
  } catch (error) {}
}
fetchingPosts();
*/
